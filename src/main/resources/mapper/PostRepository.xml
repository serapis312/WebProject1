<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lec.spring.repository.PostRepository">

    <insert id="save" flushCache="true" parameterType="com.project.childprj.domain.Post"
            useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO post
            (title, content, userId)
        VALUES
            (#{title}, #{content}, #{userId})
    </insert>

    <update id="incViewCnt" flushCache="true">
        UPDATE post
        SET viewcnt = viewcnt + 1
        WHERE id = #{id}
    </update>

    <!-- 반복해서 사용될 쿼리문은 sql 태그로 정의 하고, include 태그로 가져다 쓸수 있다 -->
    <sql id="SELECT_BASE">
        SELECT
            id,
            title,
            content,
            viewcnt "viewCnt",
            userId,
            createdate "createDate"
        FROM
            post
        WHERE
            1 = 1
    </sql>

    <select id="findById" resultType="com.project.childprj.domain.Post">
        <include refid="SELECT_BASE"/>
        AND id = #{id}
    </select>

    <select id="findAll" resultType="com.project.childprj.domain.Post">
        <include refid="SELECT_BASE"/>
        ORDER BY id DESC
    </select>

    <update id="update" flushCache="true"
            parameterType="com.project.childprj.domain.Post">
        UPDATE post
        SET title = #{title}, content = #{content}
        WHERE id = #{id}
    </update>

    <delete id="delete" flushCache="true"
            parameterType="com.project.childprj.domain.Post">
        DELETE FROM post WHERE id = #{id}
    </delete>
</mapper>










